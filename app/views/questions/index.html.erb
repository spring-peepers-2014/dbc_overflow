<h1>All Questions</h1>
<% @questions.each do |question| %>
  <b><%= link_to(question.name, question_path(question)) %></b><br><%= question.body %><br>asked by <%= link_to(question.user.name, user_path(question.user)) %> on <%= (question.created_at).strftime("%b %d '%y") %><br><br>
<% end %>
